<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <title><%= title || "連絡帳システム ログイン" %></title>
  <link rel="stylesheet" href="/css/style.css">
  <style>
    /* ===============================
       🌈 全体設定
    =============================== */
    body {
      font-family: "Noto Sans JP", sans-serif;
      background: linear-gradient(135deg, #e3f2fd, #bbdefb);
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: flex-start;
      min-height: 100vh;
      margin: 0;
      box-sizing: border-box;
      padding-top: 40px;
      overflow-y: auto;
    }

    /* ===============================
       🎓 ロゴ部分
    =============================== */
    .logo-top {
      text-align: center;
      margin-bottom: 35px;
      animation: fadeIn 1.5s ease;
    }

    .logo-top img {
      width: 180px;
      height: auto;
      border-radius: 16px;
      box-shadow: 0 4px 15px rgba(0,0,0,0.15);
      transition: transform 0.4s ease;
    }

    .logo-top img:hover {
      transform: scale(1.05);
    }

    .logo-top h1 {
      font-size: 1.8em;
      color: #0d47a1;
      margin-top: 10px;
      letter-spacing: 1.5px;
      font-weight: 600;
    }

    /* ===============================
       🔐 ログインフォーム
    =============================== */
    .login-container {
      background: #ffffff;
      padding: 40px 50px;
      border-radius: 16px;
      box-shadow: 0 4px 10px rgba(0,0,0,0.2);
      text-align: center;
      width: 350px;
      transition: all 0.3s ease;
      animation: fadeUp 1.2s ease;
    }

    .login-container:hover {
      transform: translateY(-3px);
      box-shadow: 0 6px 14px rgba(0,0,0,0.25);
    }

    h2 {
      margin-bottom: 25px;
      font-size: 1.6em;
      color: #1976d2;
      font-weight: 600;
    }

    input {
      width: 90%;
      padding: 10px;
      margin: 8px 0;
      border-radius: 6px;
      border: 1px solid #ccc;
      font-size: 1em;
      transition: 0.2s;
    }

    input:focus {
      border-color: #1976d2;
      outline: none;
      box-shadow: 0 0 4px rgba(25,118,210,0.3);
    }

    button {
      width: 95%;
      background-color: #1976d2;
      color: white;
      border: none;
      padding: 10px 0;
      border-radius: 6px;
      font-size: 1.1em;
      margin-top: 12px;
      cursor: pointer;
      transition: background-color 0.3s ease;
    }

    button:hover {
      background-color: #0d47a1;
    }

    /* ===============================
       📘 マニュアルボタン
    =============================== */
    .manual-btn {
      display: inline-block;
      background-color: #4CAF50;
      color: white;
      text-decoration: none;
      padding: 10px 20px;
      border-radius: 6px;
      font-size: 0.95em;
      margin-top: 15px;
      transition: background-color 0.3s;
    }

    .manual-btn:hover {
      background-color: #45a049;
    }

    /* ===============================
       ⚠️ エラー表示
    =============================== */
    .error {
      color: #d32f2f;
      font-weight: bold;
      margin-bottom: 15px;
      background-color: #ffebee;
      border: 1px solid #ffcdd2;
      border-radius: 5px;
      padding: 8px;
    }

    /* ===============================
       🕊 フッター
    =============================== */
    .footer {
      margin-top: 25px;
      font-size: 0.9em;
      color: #555;
      text-align: center;
      animation: fadeIn 2s ease;
    }

    /* ===============================
       ✨ アニメーション
    =============================== */
    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(-10px); }
      to { opacity: 1; transform: translateY(0); }
    }

    @keyframes fadeUp {
      from { opacity: 0; transform: translateY(20px); }
      to { opacity: 1; transform: translateY(0); }
    }

    /* ===============================
       📱 レスポンシブ対応
    =============================== */
    @media (max-width: 480px), (max-height: 700px) {
      body {
        padding-top: 25px;
      }
      .logo-top img {
        width: 150px;
      }
      .logo-top h1 {
        font-size: 1.5em;
      }
      .login-container {
        width: 90%;
        padding: 30px 25px;
      }
      h2 {
        font-size: 1.4em;
      }
    }
  </style>
</head>
<body>

  <% if (typeof student === 'undefined') { var student = { name: '', class_name: '' }; } %>

  <!-- 🎓 ロゴ -->
  <div class="logo-top">
    <img src="/images/school_logo.png" alt="学校ロゴ">
    <h1>私立○○中学校 連絡帳システム</h1>
  </div>

  <!-- 🔐 ログインフォーム -->
  <div class="login-container">
    <h2>ログイン</h2>

    <% if (typeof error !== "undefined" && error) { %>
      <p class="error"><%= error %></p>
    <% } %>

    <form method="POST" action="/login">
      <input type="text" name="id" placeholder="ユーザーID" required><br>
      <input type="password" name="password" placeholder="パスワード" required><br>
      <button type="submit">ログイン</button>
    </form>

    <!-- 📄 マニュアルダウンロードボタン -->
    <a href="/doc/School_Journal_User_Guide_Final.pdf" class="manual-btn" download>
      📘 使い方マニュアルをダウンロード
    </a>
  </div>

  <!-- 🕊 フッター -->
  <div class="footer">
    <p>© 2026 JPT Parallel Technologies</p>
  </div>

</body>
</html>
